import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ittol.inv',
  appName: 'inv-ittol-front-end',
  webDir: 'dist/inv-ittol-front-end/browser'
};

export default config;
